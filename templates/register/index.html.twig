{% extends 'base.html.twig' %}

{% block title %}Inscription {% endblock %}

{% block body %}
    <h1>Création d'un compte</h1>
    {{ form_start(form) }}
    {{ form_end(form) }}
    {% if msg is defined %}
        <p>{{ msg }}</p>
    {% endif %}
{% endblock %}
