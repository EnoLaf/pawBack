{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Ajouter un animal</h1>
    {{ form_start(form) }}
    {{ form_end(form) }}
    {% if msg is defined %}
        <p>{{ msg }}</p>
    {% endif %}
{% endblock %}
